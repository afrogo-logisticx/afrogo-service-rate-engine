services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      MODE: "local"
    volumes:
      - ./snapshots:/snapshots
      - ./ledger:/ledger
    # Optional: restart on failure
    restart: unless-stopped
